export class FurnitureFactory {
  // public furnitureType: IFurniture;

  static createFactory(furnitureType: string): IFurniture {
    if (furnitureType == "SmallChair") {
      // return an object created from the chair factory
    } else if (furnitureType == "MediumChair") {
      // return an object created from the chair factory
    } else if (furnitureType == "LargeChair") {
      // return an object created from the chair factory
    } else if (furnitureType == "SmallTable") {
      // return an object created from the table factory
    } else if (furnitureType == "MediumTable") {
      // return an object created from the table factory
    } else if (furnitureType == "LargeTable") {
      // return an object created from the table factory
    }
  }
}
